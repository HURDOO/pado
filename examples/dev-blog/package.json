{
  "name": "pado-dev-blog",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "node server.mjs",
    "test": "node --test server.test.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --test server.test.mjs"
  }
}
